<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.Movie.oh.service.ohMapper">
	<insert id="insertMember"
		parameterType="co.yedam.Movie.comm.MemberVO">
		INSERT INTO MEMBER(
					MEMID,
					MEMNAME,
					MEMPASSWORD,
					MEMADDRESS,
					MEMTEL,
					MEMGRADE,
					MEMSTATUS,
					MEMAGE,
					AUTHOR
					)
					VALUES(
					#{memId},
					#{memName},
					#{memPassword},
					#{memAddress},
					#{memTel},
					user,
					user,
					#{memAge},
					user
					)
					
	</insert>

</mapper>